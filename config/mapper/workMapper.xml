<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.iotek.ssm.dao.WorkDao">
	<insert id="goInsertWork" parameterType="Work">
		INSERT
		INTO
		work(
		time,
		gotime,
		lateness,
		absent,
		e_id
		)VALUES (
		#{time},
		#{goTime},
		#{lateness},
		#{absent},
		#{employee.id}
		)
	</insert>
</mapper>





		
